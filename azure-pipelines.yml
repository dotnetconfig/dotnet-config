variables:
- name: Configuration
  value: Release
- name: PackageOutputPath
  value: $(Build.ArtifactStagingDirectory)

stages:

- stage: Build
  jobs:
  - job: Build
    pool:
      vmImage: 'windows-2019'
    steps:
    - checkout: self
      submodules: recursive
      clean: true
    - task: UseDotNet@2
      inputs:
        packageType: sdk
        useGlobalJson: true
        performMultiLevelLookup: true

    - script: dotnet build -p:Configuration=$(Configuration)
      displayName: Build

    - script: dotnet test
      displayName: Test

    - task: PublishBuildArtifacts@1
      displayName: Upload
      condition: succeeded()
      inputs:
        PathtoPublish: $(Build.ArtifactStagingDirectory)
        ArtifactName: package
        ArtifactType: Container
